Notes & TODO:
* Add treasure helper screen to allow for adding treasure WITH links to the items that are based on high/low tier.
* Update the helper screen for encounters to check if one already exists and ask if they are sure they wish to save over it.
  If they say yes, overwrite the existing macro instead of creating a new one.
* Update the Party Roll to actually check against the party sheet for the skills being rolled and create a dropdown to select from those skills.
  Also update the helper screen to allow for selecting if proficiency is required and check against proficiency when populating the party roll.
* Add a victory points subsystem tracker that can be set up with a threshold or breakpoints and track multiple different points within the same screen.
  It needs to be able to track how many checks the player has made and how many full rounds the party has been working on the subsystem.
* Add an influence subsystem tracker that can handle multiple people being influenced within the same encounter. Track what has already been told to the players.
  Track points separately for each person being influenced and account for different DCs for different tiers and subtiers.